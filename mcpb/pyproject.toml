[project]
name = "tracehub-mcp-mcpb-launcher"
version = "0.12.2"
description = "MCPB launcher that resolves and runs the published tracehub-mcp PyPI package via uv"
requires-python = ">=3.11"
dependencies = [
    "tracehub-mcp",
]
